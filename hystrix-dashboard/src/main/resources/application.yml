server:
  port: 9232

spring:
  application:
    name: HYSTRIX-DASHBOARD

hystrix:
  dashboard:
    sproxy-stream-allow-list: "*"

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://service-registry:8761/eureka